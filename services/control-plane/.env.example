# Database
DATABASE_URL=<postgresql-connection-string>

# Server
HOST=0.0.0.0
PORT=8080

# Security (shared with dashboard CONTROL_PLANE_JWT_SECRET)
JWT_SECRET=your-control-plane-jwt-secret

# Optional: Tailscale
TAILNET_DOMAIN=tailnet-xyz.ts.net
