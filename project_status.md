# Project Status - agent-command

> **Auto-generated by [project-status agent](https://github.com/cvsloane/open-agents)**
> Last updated: 2026-07-27T15:08:41.444Z

## Purpose

This file provides context for AI assistants (Claude Code, Cursor, etc.) about recent development activity. It is automatically updated daily at 6 AM EST.

**DO NOT DELETE THE SECTIONS BELOW** - they are auto-populated by the project-status agent.

---

## Recent Development Sessions

**Active Session**: `agent-command`
**Working Directory**: `/home/cvsloane/dev/agent-command`

**Recent Terminal Output**:
```
  Whichever way, the two config changes still apply: the health
  check should target /ready (currently health_check_enabled:
  false, path /), and the dashboard needs --build-arg
  APK_SOURCE=release or it ships without the APK.
  On item #1
  I'd rather not start the terminal migration in this session. My
  context is nearly spent, and that change removes a live protocol
  fallback across the server and client together — it needs a
  full context budget to do safely, not the tail of one. Starting
  it badly is worse than starting it fresh.
  Everything else is merged, on main, and green: 4e8316b, full CI
  passing. The work is done; it just isn't deployed.
✻ Cogitated for 3m 14s
※ recap: Goal was hardening agent-command plus building the
  Termius gaps; all of that is merged to main and CI-green, but
  production still runs the old code. I stopped before deploying
  because I can't tell which Coolify resource serves
  agents.heavisidetechnology.com. Tell me which one, or deploy it
  yourself.
                           new task? /clear to save 586.6k tokens
──────────────────────────────────────────────────────────────────
❯ 
──────────────────────────────────────────────────────────────────
  Opus 5 | ctx:59% | in:585k out:929 | ~/dev/agent-command
  ⏵⏵ auto mode on (shift+tab to cycle) · ← for agents
```


---

## Recent Commits (Last 5)

| Hash | Message | Date | Author |
|------|---------|------|--------|
| `4e8316b` | Merge pull request #118 from cvsloane/feat/preview-ports-and-file-bridge | 2026-07-26 | Chris Sloane |
| `4af3cc0` | test(android): include attention in the pinned subscription contract | 2026-07-26 | cvsloane |
| `a734e04` | fix(android): handle attention events in the activity's stream branch | 2026-07-26 | cvsloane |
| `afc9b22` | feat(android): alert on agent attention while the app is backgrounded | 2026-07-26 | cvsloane |
| `85b2bd5` | docs(agentd): document the preview and file bridge configuration | 2026-07-26 | cvsloane |


---

## Files Changed Recently

- `agents/agentd/cmd/agentd/main.go`
- `agents/agentd/config.example.yaml`
- `agents/agentd/internal/config/config.go`
- `agents/agentd/internal/filebridge/bridge.go`
- `agents/agentd/internal/filebridge/bridge_test.go`
- `agents/agentd/internal/preview/ports.go`
- `agents/agentd/internal/preview/ports_test.go`
- `agents/agentd/internal/protocol/command.go`
- `agents/agentd/internal/protocol/messages.go`
- `apps/android/app/src/main/AndroidManifest.xml`
- `apps/android/app/src/main/java/com/heaviside/agentcommand/data/UiStreamModels.kt`
- `apps/android/app/src/main/java/com/heaviside/agentcommand/data/UiStreamSocket.kt`
- `apps/android/app/src/main/java/com/heaviside/agentcommand/domain/AttentionNotificationPolicy.kt`
- `apps/android/app/src/main/java/com/heaviside/agentcommand/MainActivity.kt`
- `apps/android/app/src/main/java/com/heaviside/agentcommand/service/AttentionService.kt`
- `apps/android/app/src/main/res/drawable/ic_stat_agent.xml`
- `apps/android/app/src/test/java/com/heaviside/agentcommand/data/UiStreamContractTest.kt`
- `apps/android/app/src/test/java/com/heaviside/agentcommand/domain/AttentionNotificationPolicyTest.kt`
- `apps/dashboard/src/components/session/AttachFilePanel.tsx`
- `apps/dashboard/src/components/session/PreviewPortsPanel.tsx`


---

## Branch Info

- **Current Branch**: `main`
- **Last Commit**: 2026-07-26T21:59:14-04:00
