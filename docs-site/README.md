# Docs Site

This folder contains a static Docsify site used for public documentation.
It is served by `deploy/Dockerfile.docs` and uses the Markdown files in `/docs`.
The current product entry point is [Command Center](/docs/command-center.md);
the sidebar and cover page should describe `/` as the canonical signed-in route.
