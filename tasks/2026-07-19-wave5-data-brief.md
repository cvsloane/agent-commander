# W5-DATA — Tmux Identity in SQL, Event Registry, Runtime Validation, Hygiene

Read master plan workstream F + findings §4 items 1,5,8. Worktree `/home/cvsloane/dev/wt/ac-w5-data`, branch `refactor/wave5-data-contracts`. Ownership: `migrations/037_*.sql` + `038_*.sql` (claimed), `packages/ac-schema`, `services/control-plane/**`, `apps/dashboard/src/lib/**` + `src/stores/**` (NOT components), tests. No push; handoff `tasks/massive-refactor-handoffs/w5-data.md`; token `W5-DATA FROZEN <sha>`. A sibling security lane is also editing control-plane — avoid auth/rate-limit/ticket files (routes/terminal auth block, auth/, index.ts CORS); coordinate via handoff notes if unavoidable.

1. Migration 037: sessions gain `tmux_session_name`, `tmux_window_index`, `tmux_pane_index` columns + backfill from metadata.tmux/tmux_target; CP ingest writes them from the typed upsert (agentd already sends TmuxPaneIdentity via W5-GO-CONTRACTS structs — verify field names); `/v1/tmux/roster` becomes a SQL-grouped query; dashboard `lib/tmuxRoster.ts` consumes server identity with regex reconstruction demoted to fallback (keep tests green). Pane re-adoption: on upsert of an unmanaged pane whose env/option carries a known AC_SESSION_ID, re-bind instead of minting a new session.
2. Event registry: per-event-type zod payload schemas in ac-schema (claude.hook, codex.hook, codex.event, workshop.*, approval.requested, command.completed, orchestrator.report, terminal.audit); ingest validates + Prometheus counter on unknown/invalid (warn, never drop); delete never-emitted enum values or emit them properly (approval.decided — emit it on decide). Retention: config-driven pruning job for events + session_snapshots (default 30d, off when unset) + docs.
3. Dashboard runtime validation: `parseServerToUIMessage()` (zod, tolerant of unknown types) at the WS boundary; zod-validated fetchAPI for the hot endpoints (sessions, roster, graph, launch); DELETE drifted duplicates (api.ts SessionMetrics/AnalyticsSummary/ProviderUsage/TimeSeriesPoint/BulkOperationResult/MCP types/SpawnProvider, stores/usage.ts SessionUsage, pubsub TopicType copies) re-exporting from @agent-command/schema; fix `provider-usage` vs `providerUsage` query-key drift.
4. Migration 038 hygiene: FK summaries.session_id (nullable, ON DELETE SET NULL), user_settings single identity (user_id primary), projects.repo_id FK, drop `kind='service'` from enum usage if zero rows (verify first — else leave and note); wire approval timeout helpers into a periodic sweep (helpers exist unused).
5. Tests for all of the above (registry validation, roster SQL grouping parity vs old client logic, re-adoption, retention job, runtime validation tolerance).

Gate (bare exit codes): `DATABASE_URL=postgres://test:test@localhost:5432/test JWT_SECRET=test-secret-for-gate pnpm --filter @agent-command/control-plane test && pnpm --filter @agent-command/schema test && pnpm --filter @agent-command/dashboard test && pnpm typecheck`
