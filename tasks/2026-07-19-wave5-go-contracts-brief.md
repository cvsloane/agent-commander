# W5-GO-CONTRACTS — Typed Go Protocol, Fixture Matrix, Release Engineering

Read master plan workstreams F/G + findings §4 item 2, §5 item 5. Worktree `/home/cvsloane/dev/wt/ac-w5-go`, branch `refactor/wave5-go-contracts` off latest origin/refactor/tmux-command-center. `export PATH=$HOME/.local/go/bin:$PATH`. Ownership: `agents/**`, NEW files in `tests/fixtures/protocol/`, `.github/workflows/**` (Go jobs + release), `deploy/**` (agentd install/update script). Push branch when gate green. Handoff `tasks/massive-refactor-handoffs/w5-go-contracts.md`; token `W5-GO-CONTRACTS FROZEN <sha>`.

1. **Typed protocol structs**: introduce `internal/protocol` with production Go structs for every agent↔CP message agentd sends/receives (hello, upsert incl. tmux identity + parent/rollup metadata, snapshot, events, commands dispatch/result, terminal incl. new cols/rows/resume/lag/audit, usage, acks). Replace `map[string]any` encoding and inline anonymous decode structs at call sites — mechanical, behavior-identical.
2. **Fixture matrix**: add fixture JSONs for every message type + all command payloads; Go tests decode them into the PRODUCTION structs (round-trip encode/compare); keep existing 3 fixtures passing. (TS side already validates its unions — add fixtures TS-side only as new files if trivially wired into the existing ac-schema fixture test; otherwise list in handoff.)
3. **Release engineering**: version via `-ldflags -X` single-source (kill remaining hardcoded literals); add Go build+vet+test job to CI if missing; goreleaser config (linux amd64/arm64) or equivalent build script + `deploy/install-agentd.sh` (download/replace/restart systemd unit); CHANGELOG entry.
4. Gate: `go build ./... && go vet ./... && go test ./...` bare exit codes; CI workflow YAML validated (actionlint if available, else careful review).
