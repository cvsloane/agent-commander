# Frontend Command Center UX — Program Log (append-only)

## 2026-07-20
- ~12:00Z Four-agent frontend study completed; findings written (`tasks/2026-07-20-frontend-ux-study-findings.md`). Production verified live at `70fa53e` (SOURCE_COMMIT on apps-vps containers).
- ~13:00Z Master plan authored; owner locked all 8 decisions via interview (landing=Command Center, hooks-not-CC, full modernization incl. TS 7 + Zod 4 excl. R3F/Drei, 2-up multi-terminal, one attention surface, /sessions retained, per-wave deploys).
- ~13:30Z Program launch: baseline re-verified green (typecheck+test:ci) at `70fa53e`. Acceptance checklist, status board, briefs authored. Integration branch `refactor/frontend-command-center` cut from main.
- Wave 1 lanes launched: FW1-MODERN (homelinux, codex) and FW1-TMUX-GO (heavisidelinux, codex). Fixture shapes for the tmux topology event + 8 window/pane commands frozen in the FW1-TMUX-GO brief.
- ~14:10Z FW1-MODERN froze at `76d3c5e` after 34m (attempt 1). AI Lead verification: full gate re-run green (exit 0), firewall audit clean, suppression scan clean (one grep hit was the handoff's own prose), spot-checks of SessionList/MobileLaunchSheet/R3F-type-bridge/CP lint fixes all mechanical. Notable compatibility seams (documented in handoff, accepted): `tools/eslint-typescript-compat` isolates typescript-eslint's TS 5.9 parser runtime; Next route type-gen uses `@typescript/native-preview` alias under TS 7. INTEGRATED @ `6f8ce3c` (squash).
- FW1-TMUX-GO in progress at +35m: implementing unsequenced WS send path for topology events; codex context low (12%) — auto-compaction expected, watcher armed for stall/exit.
- ~15:00Z FW1-TMUX-GO froze at `eaadfe9` (attempt 1, ~60m). AI Lead verification: Go gate re-run green on heavisidelinux, firewall clean, fixtures byte-identical to frozen shapes, supersede diff conservative. INTEGRATED @ `504d7d3`. Full wave gate (TS+Go) green on integration branch.
- ~15:20Z Independent adversarial reviewer (fresh Claude subagent vs gpt-5.6-sol builders): verdict HOLD. C1 critical: agentd emits `tmux.topology` on startup but deployed CP terminates the agent socket on unknown envelopes (closed union + socket.terminate) — the brief's "CP ignores unknown types" assumption was wrong; Builder had flagged this exact uncertainty. W3: crashed agentd pollutes user tmux hooks (accumulating ac-agentd wait-for entries, restore re-installs them). W4: stale viewers never reaped by Sweep. W2 (commands unreachable via CP) = by design, Wave 2 scope. FW1-MODERN verified clean — every paramIndex/definite-assignment/memo edit traced behavior-neutral. Supersede logic explicitly verified SAFE.
- Wave 1 HELD. R2 correction brief authored (`tasks/2026-07-20-fw1-tmux-go-corrections-brief.md`): topology emission default-off, hook snapshot filtering + startup cleanup, stale TTL sweep. CP tolerance + schema registration added to FW2-CONTRACTS scope; tmux-version gating of percent splits + kill_window confirmation carried to Wave 3.
- ~16:00Z Liveness catch: R2 re-task prompt was staged in the codex composer but the submit Enter never registered; watcher flagged a 30-min stall, AI Lead read the pane, submitted, and verified "Working" state. (Reinforces the ADL rule: always verify a re-tasked agent activated on the next cycle.)
- ~16:50Z FW1-TMUX-GO-R2 froze at `7a8e6ee` (attempt 2). AI Lead verification: Go gate green on heavisidelinux, all three fixes verified in source (single choke-point default-off gate; hook filter + live-server strip; TTL sweep with full map cleanup, bridges closed outside lock). Delta applied to integration @ `76d7a12`; final full wave gate (TS+Go) green.
- ~17:00Z Fresh-eyes reviewer re-verdict on R2 delta: SHIP — C1/W3/W4 all CLOSED with end-to-end tests; no new Warning+ issues; noted the Sweep rework removed a pre-existing lock-held-during-close hazard. Residual (informational): `ac-agentd-` hook namespace reservation.
- WAVE 1 COMPLETE. Held for owner: PR merge + deploy. agentd binary rollout deferred to Wave 2 by AI Lead ruling (CP tolerance lands in FW2-CONTRACTS).
- ~17:30Z OWNER DIRECTIVE: merge Wave-1 PR and continue autonomously through program end; owner reviews at the very end. Autonomy change recorded: per-wave PR merge + production deploy move to autonomous-with-receipt; owner on-device wave checkpoints deferred to final review.
- ~17:40Z Wave-1 PR CI: Docker build failed — two deploy-layer seams outside FW1-MODERN's firewall (deploy/** is AI-Lead-owned): base Dockerfiles didn't copy the new `tools/eslint-typescript-compat` file: package before `pnpm install --frozen-lockfile`, and dashboard Dockerfile still copied the deleted `.eslintrc.cjs`. Fixed both as AI Lead (receipt: this commit).
- ~18:20Z Wave-1 PR #86 MERGED to main (d565b00) after: Docker-build fix (AI Lead, deploy-layer seams), CodeRabbit threads resolved (its hook-gating Major was valid — fixed in 2b105bc; two others resolved with justification). Coolify deploy watcher armed. Wave 2 launched: FW2-CONTRACTS + FW2-TERM codex lanes active on homelinux (activation verified in panes — submit-Enter checked this time).
- ~18:30Z Wave-1 DEPLOYED: production containers built from d565b00 (verified SOURCE_COMMIT on apps-vps), /health ok, agent connected (old agentd binary per rollout hold).
- ~19:10Z FW2-CONTRACTS froze at `40bbd14` (attempt 1). AI Lead verification: full gate re-run green (exit 0), firewall clean (zero dashboard/agents/deploy/migrations edits; frozen fixtures untouched — byte-exact TS round-trips proven, 16/16), Zod 4 semantics preserved (partialRecord for enum records), tolerance implementation verified in source (unknown string type → rate-limited warn+drop; malformed/oversized still terminate; +1MB frame cap). INTEGRATED (squash).
- ~20:30Z FW2-TERM froze at `9b34dfb` (attempt 1): gate re-run green, firewall clean (LayoutShell diff = 2 lines, addon-search only dep, protocol untouched), per-claim tests (250-frame zero-writes, same-instance persistence, scroll anchor). INTEGRATED @ e31f2d3 (lockfile auto-merge verified via frozen install).
- ~20:45Z Wave-2 adversarial review: SHIP-WITH-NOTES, zero criticals; risk surfaces explicitly cleared (Zod semantics, tolerance ordering incl. known-type-invalid-payload still terminating, endpoint authz, terminal lifecycle, SSR). W1/W2 (fleet endpoint scan+N+1, auth-only role) + N2/N3 fixed via FW2-CONTRACTS-R2 `38c1a12` (scoped loads, bounded concurrency 4, operator role, terminate-invariant test, host_id ordering) — integrated @ bd934ef. N1/N4/N5 recorded as skipped (cosmetic/documented design). Final wave gate green.
- WAVE 2 COMPLETE. PR to main next; after deploy: agentd binary rollout on heavisidelinux proceeds (CP tolerance now live).
- ~21:20Z Wave-2 MERGED (effab04) + DEPLOYED (SOURCE_COMMIT verified, health ok). PR-review fixes: scrollback 502 on agent-side failure; board policy text synced.
- agentd rollout: new binary BUILT at effab04 and staged at /tmp/agentd-new on heavisidelinux (temp worktree /tmp/ac-build-effab04; backup ~/.local/bin/agentd.bak-fw2 exists). In-place replacement of the RUNNING binary was blocked by the local permission policy — HELD FOR OWNER: run `rm ~/.local/bin/agentd && cp /tmp/agentd-new ~/.local/bin/agentd && systemctl --user restart agentd` on heavisidelinux, then optionally set `topology_events: true` under tmux: in ~/.config/agentd/config.yaml. Program continues regardless (Wave-3 UI has roster fallback).
- Wave 3 launched: FW3-SHELL + FW3-TMUX-UI codex lanes active on homelinux (activation verified).
- ~22:20Z FW3-SHELL froze at `5bba177` (attempt 1): gate re-run green, firewall clean, net-negative diff (legacy landing + SpawnSessionDialog + OrchestratorModal duplication deleted; launch definitions centralized). Flagged cross-lane seam (useTmuxRosterData /tmux URLs) steered to FW3-TMUX-UI mid-flight. INTEGRATED @ a5788af.
- ~23:30Z FW3-TMUX-UI-R2 froze at `c2bcfbe` (attempt 2): clean rebase onto integration tip, 4 fix commits (topology TTL 30s w/ stale-live eviction — verified in diff; single roster feed; last-window confirm gated on authoritative topology; percent splits require reported tmux version). INTEGRATED @ 86ce697. AI Lead separately fixed reviewer W3+N5 (canonical internal links, dead SettingsButton) @ af8352e.
- Final wave-3 gate initially failed twice on a smoke webServer tailwind @config resolution error; orchestrator session died mid-diagnosis (runaway dev-server probe). Root cause found on resume: STALE `.next` Turbopack cache in the main repo vs wave-3 route restructure — fresh compile clean after cache removal. Lesson: clear `apps/dashboard/.next` in the integration repo after route-topology changes.
- WAVE 3 COMPLETE: final gate green after .next cache fix (14/14 smoke). Reviewer verdict SHIP-WITH-NOTES; W1/W2/N1/N4 closed in R2, W3/N5 by AI Lead, N2/N3/N6 recorded-skipped (self-healing/cosmetic). PR to main next.
- ~00:40Z (07-21) Wave-3 PR #88 MERGED (f3df15d) after CodeRabbit round: 3 fixes (idle/active bucket exclusivity, exact nav matching, confirm on every topology window close — matching native tmux confirm-before), 2 resolved by design (ephemeral secondary terminal; stale-topology timer carried to FW4-FLEET brief). Deploy watcher armed. Wave 4 launched: FW4-FLEET + FW4-SURFACES active (verified).
- Wave-3 DEPLOYED (f3df15d verified on apps-vps, health ok). The production app now lands on the Command Center.
- ~01:10Z Owner steering: distribute Builder lanes across both machines (heavisidelinux tmux for agents too) and keep tmux clean. Applied: killed 5 completed lane windows on homelinux + 1 on heavisidelinux; removed 6 merged worktrees (verified clean; both machines) + the /tmp build worktree. Wave-5 lane assignment updated: FW5-QA runs on heavisidelinux.
- ~01:30Z Owner report: no way to add a second host (homelinux cannot attach). AI Lead attempted operational enrollment; local permission policy blocked credential provisioning (JWT mint, token/hash generation) AND daemon installation — respected, no workarounds. Product fix steered into FW4-SURFACES (owns /hosts): Add Host dialog → POST /v1/hosts → one-time token panel + install instructions + Rotate Token action. HELD FOR OWNER (operational enrollment of homelinux): after Wave 4 deploys, click Add Host in /hosts (admin session), then on homelinux: build agentd (`cd ~/dev/agent-command/agents/agentd && go build -o ~/.local/bin/agentd ./cmd/agentd`), write ~/.config/agentd/config.yaml (mirror heavisidelinux's; new host id + token; tmux socket /tmp/tmux-1000/default; consider topology_events: true), install the user systemd unit (copy heavisidelinux's ~/.config/systemd/user/agentd.service), `systemctl --user daemon-reload && systemctl --user enable --now agentd`.
- ~02:00Z homelinux ENROLLED as host (owner-executed script via ! prefix): host_id bc9fc6db, DB rows via db-vps superuser channel (hashes only), agentd built from current main, user systemd service active, topology_events: true — "tmux topology hooks active" in journal, /health agents:2. homelinux is the canary for the new agentd binary; heavisidelinux rollout still staged for owner. Add Host UI remains in FW4-SURFACES scope.
- ~02:30Z heavisidelinux agentd ROLLED OUT (owner-executed atomic swap via ! prefix; AI Lead enabled topology_events + restart): new binary active, "tmux topology hooks active", /health agents:2. FULL FLEET now on the new agentd with live topology. All operational held-items closed; remaining owner action: end-of-program review only.
- ~03:10Z FW4-FLEET froze at `9081a13` (attempt 1): gate re-run green (408 tests, 15/15 smoke), firewall clean, no deps, carried topology-TTL-fallback item covered by tests. INTEGRATED (squash).
- ~04:40Z Wave-4 gate green: lint/typecheck/test:ci in-session, smoke (20/20, 1.0m) + build via tmux TTY after this session's shell began swallowing Playwright child output (empty logs, silent hangs — environment issue, NOT the suite; earlier "failures" were this + the 10-min command cap). LESSON: run Playwright gates in a tmux TTY window. R2 rounds integrated: FW4-SURFACES-R2 295c9c0 (Ctrl+K exemption as tested pure fn, token reset, ws/wss derivation) @ 8ef7e33; FW4-FLEET-R2 f0d7b89 (freshness guard, cache pruning, read-only gating) @ e179477 (lessons.md union-merge resolved).
- WAVE 4 COMPLETE. Reviewer SHIP-WITH-NOTES → all three warnings closed in R2s; nitpicks closed (rotate reset, scheme) or accepted. PR next.
- ~05:20Z Wave-4 PR #89 MERGED (fa0ba72; CodeRabbit was rate-limited — no findings, our own review chain already ran). Deploy watcher armed. Wave-4 tmux windows + worktrees cleaned (both machines policy). WAVE 5 LAUNCHED: FW5-POLISH (homelinux) + FW5-QA (heavisidelinux, per machine-split directive) — both verified active. Watch item: heavisidelinux codex weekly quota at 10%.
- Wave-4 DEPLOYED (fa0ba72 verified, health ok). Add Host flow, fleet unification, attention overlay, composer, palette all live.
- ~06:30Z FW5-POLISH froze at `9ceed37` (attempt 1): gate re-run green in tmux TTY (20/20 smoke, 145 dashboard tests), firewall clean, web-vitals the only (allowed) dep. INTEGRATED (squash). Gate window killed post-run per owner reuse-panes directive.
- ~07:50Z WAVE 5 COMPLETE + PROGRAM GATE GREEN at d7583f2 (198 CP + 145 dashboard tests, 20/20 smoke, 14/14 journeys, build, Go). Wave-5 review SHIP-WITH-NOTES: MEDIUM was AI Lead brief error (JSON fallback deletion) — fallback restored, full binary-only migration backlogged; CHANGELOG corrected; LOW/INFO items backlogged. Incident: gate command mis-keyed into a live owner codex pane (pane-index assumption) — caught before submission, composer cleared, pane-targeting rule added to memory. FW5-QA honesty note: flagged 4 NOT-METs rather than rubber-stamping; 2 resolved at integration, 2 remain for owner final review (on-device PWA pass; docs-site screenshot backlogged).
- ~23:05Z Wave-5 PR #90 MERGED (6e928a4) + DEPLOYED (SOURCE_COMMIT verified; both agents reconnected after CP restart). Tagged v0.4.0. Wave-5 windows/worktrees cleaned. PROGRAM COMPLETE — Final Report: tasks/frontend-ux-final-report.md.
- ~00:30Z (07-21) PROGRAM REOPENED for WAVE 6 (owner directive): mobile tmux UX, run as Wave 6 of this program. 8 decisions locked via interview (ultra-minimal rail default + config engine, 14px font, letterbox-when-desktop-attached, attach-everywhere + cold-open restore, one-status-row full-bleed, command marks in batch 2, Hermes next, single end on-device check). Plan: tasks/2026-07-20-mobile-tmux-ux-plan.md. Batch 1 launching: FW6-CANVAS (homelinux) + FW6-FLOW (heavisidelinux, cross-stack). Batch 2 (FW6-PRECISION + FW6-VERIFY) briefs at batch-1 integration.
- ~01:40Z FW6-CANVAS froze at `a593fc6` (attempt 1): gate re-run green (21/21 smoke incl. asserted ≥40-rows-at-14px + rail-in-keyboard-inset tests at 412×915 Android), firewall clean (two in-spirit paths accepted: top-level TerminalView.tsx, SessionDefaultsPanel for new settings). INTEGRATED (squash).
- ~03:30Z (07-21) WAVE 6 BATCH 1 GATE GREEN @ c181aeb (21/21 smoke, 15/15 journeys, full TS+Go). Integration surfaced and fixed 3 real composition defects: letterbox resize feedback loop (idempotence key + scrollbar-gutter), key rail intercepting attention-overlay taps (overlay now docks above via measured --terminal-rail-height), settings-sync PUT blanking the app on mock 404 (mock taught; fail-soft carried to FW6-VERIFY). Plus 2 test-reality drifts (auto-attach choreography; hidden quick-switch text twin → scoped locators). Port-squatting leftovers cleaned. Adversarial review of batch 1 next; batch-2 briefs after verdict.
- ~04:20Z Batch-1 adversarial review: SHIP-WITH-NOTES. W1+W2 were in the AI Lead's OWN seam fixes (letterbox font-sync gap; overlay CSS var on a sibling = no-op — LESSON: AI-Lead fixes need the same verification rigor as lane work; the "passing" tap test was actually the overlay covering the rail). W3: crash-path window-size pin leak on shared windows. All fixed + N2 (one-shot Ctrl disarms on IME text, escape sequences keep it armed — refined via Builder's own test) + N4 (dead TmuxKeyBar deleted). Closing gates green: 21/21 smoke, 16/16 journeys. BATCH 2 LAUNCHING: FW6-PRECISION (homelinux) + FW6-VERIFY (heavisidelinux, light lane for quota).
- ~06:40Z FW6-PRECISION froze at `adc996e` (attempt 1, 11 commits): gate re-run green incl. journeys, firewall clean (agentd touch = flagged shell-integration preamble with Go test). INTEGRATED (squash). VERIFY-R2 continuing on unblocked path.
- ~08:10Z FW6-VERIFY-R2 froze at `315c828` (attempt 4 across 2 honest holds — both surfaced real defects: dev-overlay tap interception; descriptor dropping hostId). All carried items + docs + 0.4.1 + device checklist delivered on 3% quota. INTEGRATED @ c7141be. WAVE-6 FINAL GATE GREEN: 21/21 smoke, 27/27 journeys, full TS+Go. Batch-2 adversarial review in flight.
- ~08:50Z Batch-2 adversarial review: SHIP-WITH-NOTES. HIGH: allow-passthrough set at server scope (-s) where it's a pane option — OSC-133 command marks were silently inert; fixed to -p with scope-asserting test. MED gesture-timer collision (450/500ms) fixed via cursor-armed cancellation; pointercancel activation guard; per-frame query parse cached (then made test-safe after a red test slipped one commit — grep-masked exit code, lesson noted). LOWs backlogged. CLOSING GATE GREEN. WAVE 6 COMPLETE — PR next; owner device checklist (docs/device-checklist.md) is the final acceptance item post-deploy.
- ~03:40Z (07-21) WAVE 6 MERGED (e8b9ce7, PR #91 — which turned out to be the never-merged close-out PR reused by gh; retitled) + DEPLOYED (SOURCE_COMMIT verified, agents:2). Tagged v0.4.1. agentd v0.4.1 binaries built on BOTH machines, staged for owner swap (policy). Remaining: owner binary swap + device checklist = program truly closed.
- ~03:47Z agentd v0.4.1 ROLLED OUT fleet-wide (homelinux via owner ! command; heavisidelinux completed by AI Lead after the wrapped-line mv failure — swap succeeded, reconnected). Both hosts on the Wave-6 binary. ONLY REMAINING ITEM: owner device checklist (docs/device-checklist.md).
- ~04:20Z DEVICE-CHECKLIST FINDINGS (owner, S25 Ultra): (1) terminal attach/detach loop — letterbox dims were in the terminal identity key, fed by live topology that our own agentd pin/release mutates; fixed (key = session+pane, dims frozen per attachment), hotfix PR #92 merged + DEPLOYED (fbac2dd). (2) "settings saved locally" — production PUT /v1/settings 500: user_settings_legacy_subjects owned by postgres with no grant to agent_console (superuser-run migration gap); GRANT applied to prod + migration 039 recorded. Lesson: migrations run as superuser MUST include app-role GRANTs; device testing catches what emulation structurally cannot (both bugs required a real desktop-attached/production-DB environment).
- ~05:10Z (07-21) DEVICE-CHECKLIST FINDING 3 (owner): multi-pane windows show ALL panes compressed on mobile; window switches via header picker slow (full reattach). FW6-FOCUS lane launched (brief d49682e) and froze at `a62bef5` (attempt 2, 7 impl commits): viewer-scoped `terminal.navigate` (select_window/select_pane/zoom) executed against the grouped VIEWER session (phone nav no longer yanks desktop's active window), same-session switches keep descriptor/WS/xterm instance (no reconnect), mobile Focus mode = channel-owned tmux zoom with auto-focus on multi-pane attach + guaranteed unzoom on detach/supersede/sweep/close, letterbox pin migrates across viewer window switches. AI Lead review: SHIP with 1 integration fix — lane spelled the attachment-key NUL separator as a literal byte, making TmuxTerminalWorkspace.tsx binary to git (fixed to escape sequence, c00cf86). Journey assertions STRENGTHENED (asserts no new terminal WS + legacy select_window path unused). INTEGRATED @ 3c4f715; FULL GATE GREEN (508 unit, 21/21 smoke, 28 journeys, build, Go incl. real-tmux private-socket suite). Lane window/worktree + stale fw6-canvas leftovers cleaned. PR next.
- ~06:00Z (07-21) FW6-FOCUS SHIPPED: PR #93 MERGED (main 7b21b26, checks-then-admin) + DEPLOYED (SOURCE_COMMIT verified, health ok, agents:2). agentd FW6 binaries built + rolled on BOTH hosts by AI Lead (no classifier blocks this round; staged short-path /tmp/agentd-fw6). Incident during rollout: heavisidelinux entered a ~3s register/kick loop — root cause was an ORPHAN agentd (no --config, un-parented by systemd, leftover of the owner's hung v0.4.1 background swap chain) fighting the service process for the host token; killing it also unblocked that hours-old chain (harmless — hashes verified identical to FW6 build afterward). Fresh-window verification: 0 read errors, 0 CP churn. LESSON: after any manual/`!` agentd rollout, `pgrep -a agentd` — exactly one process per host; hung background chains can fire much later. Owner phone re-test of finding 3 (pane Focus + instant window switching) is the acceptance step.
- ~06:50Z (07-21) DEVICE FINDING 4 (owner): cannot swipe-scroll attached tmux history on mobile. AI Lead diagnosis (two empirical probes, byte-level): attached tmux client is ALWAYS on the alternate screen (smcup on every attach) where xterm scrollLines() is a no-op — live swipe scroll-back NEVER worked in attached mode; pre-Wave-6 the stream view (DOM scroll) masked it, attach-everywhere+Focus exposed it. Both hosts have tmux mouse on: probe proved SGR wheel reports fed to the client PTY scroll copy-mode even while ZOOMED, exit cleanly at bottom, and pass through to alt-screen TUIs (codex transcript) — native tmux semantics; desktop trackpad already works via xterm wheel→mouse-report translation, touch is the only gap. Second defect found during diagnosis: post-navigate History pager captures the ORIGINAL attachment session (TerminalView renders from attachmentDescriptor). FW6-SCROLL lane launched (brief 7172477): touch → SGR wheel dispatcher (gated on alt-buffer + mouseTracking + writable; scrollLines fallback preserved) + historySessionId threading. Pure dashboard change, no protocol/agentd.
- ~07:40Z (07-21) FW6-SCROLL froze at `3f085d9` (attempt 1, 18 min, 4 impl commits): SGR wheel dispatcher (5-lines-per-report remainder mapping, touch-cell targeting, full gating matrix unit-tested) + historySessionId threading. AI Lead review: SHIP clean, firewall clean. INTEGRATED @ 4579e8e. Gate attempt 1 red: 1 journey flake (roster attach/type input race — passed 10/10 isolated). Deep-dive surfaced a LATENT test race: scrollback-pager journey asserted the OLDEST line right after open, passing only when the bottom-anchor rAF lost to layout — product behavior correct, test timing-sensitive (1/10 failure isolated). Fixed deterministically (assert count, pin viewport top, then content; 10/10 green). Launch-rail mobile timeout confirmed pure load flake (10/10 isolated green) — journey-suite stabilization pass backlogged. GATE ATTEMPT 2 GREEN (full chain). Lane window/worktree cleaned. PR next; no agentd changes, no binary rollout needed.
- ~13:45Z (07-21) FW6-SCROLL SHIPPED: PR #94 MERGED (checks passed, main b98a93a) + DEPLOYED (SOURCE_COMMIT verified, health ok, agents:2). No agentd/binary changes. Owner phone swipe test on an attached pane = acceptance for device finding 4.
- ~14:15Z (07-21) DEVICE FINDING 5 (owner): scroll "barely works" — gesture conflicts. AI Lead triage: (1) 5-line wheel quantization (~125px per jump + dead zone), (2) every tap focuses xterm textarea → Android keyboard pops → PTY resize mid-read, (3) 450ms cursor-arm hijacks slow scroll starts into arrow-key spam. Owner interview locked: agentd-native scroll op over terminal.navigate (copy-mode scroll-up/down N with pane-state dispatch: in-mode / alt+mouse app passthrough via send-keys -l SGR / alt no-mouse arrows / live-view copy-mode entry), explicit keyboard rail key (inputMode none by default on mobile), cursor-drag armed by rail key only (450ms timer DELETED). FW6-TOUCH lane launched (brief ed501a0). agentd rollout will be required.
- ~15:30Z (07-21) FW6-TOUCH froze at `3008c25` (attempt 1, ~35 min, 5 impl commits, full green gate incl. Go). AI Lead review: SHIP with 1 integration fix — alt+mouse wheel branch divided count/3 STATELESSLY, so frame-coalesced 1-2-line messages always floored to 0 events (slow drags inert in codex panes — the exact symptom being fixed); added per-bridge signed wheelResidue accumulation (reset on window/pane select) + pure-helper Go test (eddb27f). Also stabilized the roster attach/type journey for real (gate on Connected label, 10/10 isolated) after it burned a second gate cycle. GATE ATTEMPT 2 FULLY GREEN. Lane + builder tmux session cleaned. PR next; agentd rollout required on both hosts.
- ~16:10Z (07-21) FW6-TOUCH SHIPPED: PR #95 MERGED (checks passed, main b835f78) + DEPLOYED (SOURCE_COMMIT verified, health ok, agents:2). agentd binaries rolled on BOTH hosts by AI Lead — pgrep single-process check applied per lesson (1 process each), reconnects clean. Owner phone test = acceptance for device finding 5: swipe-scroll a codex pane (smooth 1:1), tap without keyboard pop-up, Keyboard/Cursor rail keys.
- ~16:40Z (07-21) ORCHESTRATION TRANSFERRED to heavisidelinux (owner directive, Fable quota): handoff doc tasks/2026-07-21-orchestration-handoff-heavisidelinux.md (48175ab), repo synced there, fresh Claude Code (Fable 5) in tmux agent-command:0 primed with takeover prompt and verified reading — instructed to summarize and WAIT for owner. Note for new Lead: stale windows/sessions on heavisidelinux left uncleaned per kill-session ban (agent-command windows fw5-qa-tests, fw6-flow idle codex; sessions fw6-doc-shot, fw6-r3-journeys). homelinux AI Lead signing off.
- ~17:55Z (07-21) DEVICE FINDING 6 (owner, new AI Lead on heavisidelinux): FW6-TOUCH server-side scroll REJECTED on device — "slow, error-filled, unusable for the most part"; owner benchmark is the pre-Wave-6 local DOM scroll (smooth, artifact-free, size-limited). Owner interview locked direction: LOCAL-FIRST history overlay (native on-device scroll, capture-pane range paging, touch never injects tmux input). AI Lead diagnosis: server path bounded by RTT + display-message/send-keys subprocess execs + full alt-screen repaint per step; artifacts (codex panes, transient while scrolling) = torn copy-mode repaints + read-state-once byte-leak race. KEY probe: codex runs on the tmux NORMAL screen (alt=0) with real history (5.4k lines, clean deep capture) → ONE uniform capture-pane feed serves shell and codex panes; no protocol/agentd change, no binary rollout. FW6-HIST lane launching on homelinux (brief tasks/2026-07-21-fw6-hist-brief.md): TerminalHistoryOverlay + touch dispatch rerouted to overlay + tmux-path navigate/SGR emission deleted. Pure dashboard change.
- ~21:05Z (07-21) FW6-HIST froze at `d94d31d` (attempt 1, ~21 min, 4 impl commits, full green gate from the lane TTY). Lane ran LOCALLY on heavisidelinux (classifier blocked remote codex launch over ssh; local codex quota had reset to 89%; host wrapper supplies yolo flags — plain `codex` here). AI Lead review: firewall CLEAN; adversarial reviewer SHIP-WITH-NOTES (0 critical). Integration fixes applied by AI Lead (cc9c51a + 68afa01): (1) DESIGN DEFECT FROM THE BRIEF — open gesture was spec'd swipe-UP, colliding with the upward dismiss overscroll and inverting natural content-follows-finger scrolling; flipped to downward drag with a single pure gate (shouldOpenHistoryOnGesture) + both journey directions pinned; (2) reviewer W1: prepend compensation now reads live scrollTop inside rAF (no jump-back when scrolling during load-older); (3) reviewer W2: loadingOlder state reset on close-mid-fetch; (4) short histories bottom-anchored (mt-auto); (5) restored History-dialog pane-actions journey + upward-no-op journey (reviewer W3/N7). OWNER NOTE (reviewer W4, inherited): capture-pane range addressing is relative to the moving history end — browsing history while a pane streams can duplicate a band of lines at page seams (same semantics as the History dialog; needs absolute addressing to fix). INTEGRATED @ 3699e53 (--no-ff). FULL GATE GREEN on integration (attempt 3; attempts 1-2 were THIS HOST's stale environment, not code: main repo node_modules missing the FW1 workspace lint package → pnpm install; stale .next/dev types from pre-Wave-6 routes → cache cleared per standing lesson). 200 CP + 232 dashboard tests, 21/21 smoke, 36 journeys, build. No agentd/protocol changes — no binary rollout. PR next.
- ~21:36Z (07-21) FW6-HIST SHIPPED: PR #96 MERGED (main ef5abe3; merge required OWNER `!` command — this host's permission policy blocks gh pr merge for the AI Lead; CodeRabbit thread resolution was the initial GraphQL blocker, resolved via API) + DEPLOYED (SOURCE_COMMIT ef5abe3 verified on BOTH containers, health ok, agents:2). No agentd change — no binary rollout. Owner phone test = acceptance for device finding 6: drag DOWN on an attached terminal (shell or codex pane) opens the local history overlay — native momentum scroll, auto-loads older 500-line pages, Live pill / swipe-up-at-bottom returns instantly to live; plain taps still never summon the keyboard. Known caveat to observe: possible duplicated line band at page seams if the pane streams while browsing (inherited capture-pane relative addressing; backlog candidate: absolute addressing).
- ~22:20Z (07-21) DEVICE FINDING 6b (owner): FW6-HIST overlay = BLACK SCREEN on their pane. AI Lead root cause via PRODUCTION DATA (CP audit_log rows → session 268855f4 → tmux pane %3 on heavisidelinux): the pane runs claude-code on the ALTERNATE screen (alt=1, mouse=1, history_size=1) — alt-screen apps write nothing to tmux history; the capture returns ONE line, bottom-anchored on black. Local repro chain all green (mock render + 6x-CPU-throttle/600ms-latency anchor test + screenshot) — the client is correct; the FW6-HIST brief over-generalized the codex probe (codex=normal screen) to all TUIs. Claude-code panes are un-scrollable in native tmux too; desktop trackpads scroll them via SGR passthrough to the app. Owner interview locked: HYBRID AUTO-DETECT — local overlay for history-rich panes; thin-history panes route swipes to the deployed navigate scroll op (agentd per-message pane-state dispatch = safe for misclassification; branch B delivers SGR to claude, which has mouse on). Classification = non-empty-line count of the overlay's own newest-page fetch (>=40 → overlay), primed at attach, cached per historySessionId. No agentd/protocol change, no rollout. FW6-ALT lane launching locally (brief tasks/2026-07-21-fw6-alt-brief.md). FW6-HIST acceptance stands for codex/shell panes (owner retest pending).
- ~23:15Z (07-21) FW6-ALT froze at `f1112d6` (attempt 2 honest — attempt-1 red was journey assertions around the new prime fetch, fixed + isolated x5). AI Lead verification: firewall CLEAN; adversarial reviewer SHIP-WITH-NOTES (0 critical; read-only navigate gating verified triple-layered; coalescer byte-identical to 92d98d3; claude-journey frames pinned to the real WS). AI Lead review fixes applied (3dd5b9a): coalescer reset restored in effect cleanup (pending-scheduled state deadlocked app-scroll after an enabled toggle), prime fetch try/catch + gated to touch viewports (desktop no longer issues unused captures), classification survives spinner dismiss, NEW journey for the unclassified-thin auto-close path (mutation-resistant both directions). Reviewer nitpicks recorded-accepted: empty-success→app-scroll (safe via agentd per-message dispatch), slice(-2) mobile assertion (prime fetch is legitimate there). INTEGRATION: full-gate red x2 on the prepend journey under FULL-SUITE LOAD ONLY (isolated x5 green) — root-caused as PRODUCT defect: Chrome native scroll anchoring races manual prepend compensation → double-compensation jump; fixed with overflow-anchor:none on the overlay scroller (8dd6b59) — also removes a real on-device jump. INTEGRATED @ eb72d17 (--no-ff x2; reset-hard blocked by host policy). FULL GATE GREEN attempt 3: 200 CP + 243 dashboard tests, 21/21 smoke, 38 journeys, build. No agentd change — no rollout. PR next.
- ~23:26Z (07-21) FW6-ALT SHIPPED: PR #97 MERGED (main ea86b3e; owner ! command — host policy still reserves gh pr merge; CodeRabbit thread resolved via GraphQL first) + DEPLOYED (SOURCE_COMMIT ea86b3e on BOTH containers, health ok, agents:2). No agentd change, no rollout. Owner phone test = acceptance for finding 6b: drag DOWN on a claude-code pane → the app scrolls its own transcript (per-pane truth via the deployed navigate op); drag DOWN on a codex/shell pane → instant local history overlay (finding 6 acceptance also still pending owner retest there). Program state: findings 4/5/6/6b all shipped; awaiting owner device verdict.
- ~23:55Z (07-21) DEVICE FINDING 6c (owner): codex overlay ACCEPTED; claude app-scroll REJECTED ("weird characters along the side", "poor scrolling", no improvement). AI Lead empirical probe (scratch claude TUI, 120-line transcript): SGR wheel delivery WORKS — claude scrolls ~2 lines/event, pane byte-clean at 40 rapid events, sgr=1/all=1 encoding correct — so the artifacts are VIEWER-side xterm tearing of streamed full-screen redraws + RTT feel; input tuning cannot fix it. Owner locked: local CHAT-TRANSCRIPT overlay for claude panes sourced from claude-code's own on-disk history (owner's insight), with app-scroll kept as fallback. Feasibility verified: transcript JSONLs exist per session under ~/.claude/projects/<slug>/, claude hooks already deliver transcript_path into agentd (resolveHookSessionID/extractAssistantText). FW6-CHAT lane launching locally (brief tasks/2026-07-21-fw6-chat-brief.md): capture_transcript command (schema+agentd+CP, additive, path-bounded to ~/.claude/projects) + overlay chat mode + formatter. First protocol change since FW6-TOUCH — agentd rollout will be required; deploy order safe (old agentd → graceful app-scroll fallback).
- ~00:30Z (07-22) FW6-CHAT froze at `6226466` (attempt 1, ~23 min, 5 impl commits, full green gate incl. Go). Firewall CLEAN. Adversarial reviewer SHIP-WITH-NOTES — and it COMPILED ATTACK PROBES against the file-read boundary: symlink-inside-dir, prefix-cousin, hostile cwd slugs, non-regular files ALL REJECTED (EvalSymlinks-before-Rel containment held). AI Lead review fixes (231417c): (1) torn mid-append tail no longer fails the newest page — count excludes it, corrupt interior lines become skipped stubs (claude writes these files LIVE; without this, chat mode silently died to app-scroll during active streaming); (2) toolUseResult dropped + ALL non-metadata strings under a 32KB entry budget + scanner cap 64MiB→8MiB (1MiB Bash stdout previously rode to the phone uncapped); (3) CP source field passthrough-string not enum (anti-additive 5xx risk). Recorded-accepted: first-gesture spinner on transcript-less TUI panes (vim/htop — owner usage is claude/codex; tuning-pass candidate), TOCTOU-on-owned-host, code-point wrap widths. INTEGRATED @ 30a23b9 (--no-ff). FULL GATE GREEN attempt 1: 208 CP + dashboard suites, 21/21 smoke, 40 journeys, build, Go. PR next; agentd ROLLOUT REQUIRED on both hosts after deploy (deploy order safe: old agentd → graceful app-scroll fallback).
