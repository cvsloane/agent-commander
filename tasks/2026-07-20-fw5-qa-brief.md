# FW5-QA — Journey suite, acceptance reconciliation, docs, 0.4.0 release prep (Wave 5)

Lane: FW5-QA · Machine: heavisidelinux · Worktree: `~/dev/wt/ac-fw5-qa` · Branch: `refactor/fw5-qa` (off `origin/refactor/frontend-command-center`; PUSH to origin regularly)
Plan: Workstream G · Acceptance: checklist Wave 5. Setup: `pnpm install && pnpm exec playwright install chromium` (first Playwright use on this machine).

## Work items

1. **Journey suite.** A dedicated `tests/journeys/` Playwright suite (own config file modeled on `playwright.dashboard.config.ts`, mocked control-plane routes like the smoke suite) covering the seven program journeys at BOTH 390×844 and 1280×720: signin→Command Center first paint; roster→attach→type→detach; window create/rename/kill (incl. last-window confirm); scrollback history open + load-older; take-control handoff (read-only viewer → take control); launch rail spawn; attention approve from the terminal overlay. Add a `test:journeys` root script. Keep it deterministic (no real hosts).
2. **Acceptance checklist reconciliation.** Fill EVERY checkbox in `tasks/frontend-ux-acceptance-checklist.md` across waves 1-5 with evidence (frozen SHA, test name, or handoff reference) — or flag honestly as NOT-MET with why. This file is the program's final proof surface; do not rubber-stamp.
3. **Docs truth pass.** Update `docs/` + `docs-site/` + `README.md` for the new UX: Command Center landing, window strip/pane management, multi-terminal, composer/overlay, add-host enrollment flow, command palette, keyboard shortcuts. Document the reserved `ac-agentd-` tmux hook namespace in the agentd docs (`agents/agentd/README` or docs equivalent). Remove/rewrite anything describing the deleted v0.2.0 landing or `SpawnSessionDialog`.
4. **CHANGELOG 0.4.0.** Write the `[0.4.0]` entry in Keep-a-Changelog style covering waves 1-5 (Added/Changed/Fixed/Security as applicable), dated for release day.
5. **Perf budget notes.** A short `docs/performance.md` (or existing equivalent) recording the terminal hot-path invariants (zero store writes per output frame, scroll-anchor behavior) and the fleet store reconciliation contract, so future changes have a named bar.

## Ownership firewall

You may edit: `tests/journeys/**` (new), a new root `playwright.journeys.config.ts`, root `package.json` scripts section ONLY (`test:journeys`), `docs/**`, `docs-site/**`, `README.md`, `CHANGELOG.md`, `agents/agentd/README*`, `tasks/frontend-ux-acceptance-checklist.md`. You may NOT edit: `apps/dashboard/src/**` (if a journey needs a test-id hook, list it in your handoff for the AI Lead instead), `packages/**`, `services/**`, `agents/**` code, `tests/smoke/**`, `deploy/**`.

## Gates

`pnpm lint && pnpm typecheck && pnpm test:ci && pnpm test:journeys && pnpm build` — run Playwright from a real TTY (tmux window). Commit per work item, prefix `test(journeys):` / `docs:`. ≤3 attempts per failure then hold.

## Done

Handoff `tasks/frontend-ux-handoffs/fw5-qa.md`, committed and pushed, then print exactly:
`FW5-QA FROZEN <full-sha>`
